const yesIfEven = (x) => {
  return 
}

const countXs = (s) => {
  return s.substring
}

const timesTable = (nums) => {
  if (emit())
}

const containsX = () => {
  return s.substring('x') === true
}

const sumSquares = () => {

}