const yesIfEven = (x) => {
  return 
}

const countXs = (s) => {
  return s.substring
}

const timesTable = (x) => {
  for (let i = 0; i < x.length; i++) {
    for (let j = 0; j < x.length; j++) {
      table[i][j] = (i + 1) + (j + 1);
    }
  }
};

const containsX = () => {
  return s.substring('x') === true
}

const sumSquares = () => {

}