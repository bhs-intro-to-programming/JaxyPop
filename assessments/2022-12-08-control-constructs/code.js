const yesIfEven = (x) => {
  return 
}

const countXs = (s) => {
  return s.substring
}

const timesTable = (table) => {
  for (let i = 0; i < table.length; i++) {
    for (let j = 0; j < table.length; j++) {
      table[i][j] = (i + 1) * (j + 1);
    }
  }
};

const containsX = () => {
  return s.substring('x') === true
}

const sumSquares = () => {

}