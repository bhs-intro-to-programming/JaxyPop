const getX = (object) => {
  return   object.x
}

const point = (x,y) => {
  return  { x: x, y:y };
}

const emptyObject = () => {
  return {}
} 

const distance = (x,y) => {
  let point = { 'x': 10, 'y': 20 }
}